\section{Čo sú umelé neurónové siete}
  Cieľom umelých neurónových sietí (\acrshort{uns}) je napodobniť štruktúru a~spôsob fungovania ľudského mozgu. Ten je na vonkajšej strane pokrytý mozgovou kôrou, ktorá obsahuje obrovské množstvo neurónov (v literatúre sa uvádza číslo približne $10^{10}$~\cite{mehligMachineLearningNeural2021}), ktoré sú vzájomne prepojené axónmi cez spojenia tvorené synapsami na jednej strane a~dendridmi na druhej, vďaka čomu dochádza k~prenosu informácií v~podobe elektrického signálu. Jeden ľudský neurón môže byť prepojený s~tisíckami iných~\cite{mehligMachineLearningNeural2021}. Centrálna nervová sústava je schopná paralelne spracúvať veľké množstvo informácií z~receptorov, pracovať s~pamäťou a~učiť sa z~poznatkov, názorných aj analogických príkladov, zovšeobecňovať, konkretizovať a~v~konečnom dôsledku generovať riadiace pokyny pre ostatné časti tela.

  Elementárnym prvkom \acrshort{uns} je \emph{umelý neurón}. Zjednodušený model umelého neurónu, používaný v~podstate dodnes, bol predstavený ešte v~roku 1943 v~práci McCullocha a~Pittsa~\cite{mccullochLogicalCalculusIdeas1943}. Jeho grafické znázornenie môžeme vidieť na obrázku~\ref{fig:neuron}. Matematicky je možné vyjadriť vstupno-výstupnú závislosť umelého neurónu ako
  \begin{equation}
    y = f\left( \sum^n_{i=1} w_i\, x_i - \theta \right ) = f(a),
  \end{equation} 
  kde $x_i$ sú vstupy do neurónu, $w_i$ váhy synaptických prepojení smerujúcich do neurónu, $\theta$ je prah neurónu (nazývaný tiež \textit{bias}),  $a$ predstavuje vnútornú aktivitu, $f(a)$ aktivačnú funkciu a~$y$ je výstup z~neurónu.

  \begin{figure}[b!]
    \centering
    \includegraphics[width=0.55\textwidth]{img/neuron.tex}
    \caption[McCulloch-Pittsov model neurónu.]{McCulloch-Pittsov model neurónu. $x_i$ sú vstupy neurónu, $w_i$ váhy synaptických spojení, $\theta$ je prah neurónu, $f(a)$ predstavuje aktivačnú funkciu, $a$ vnútornú aktivitu, $y$ je výstup z~neurónu.}\label{fig:neuron}
  \end{figure}

  \acrshort{uns} sa skladajú z~množstva neurónov, ktoré sú usporiadané do vrstiev. Prvá vrstva sa nazýva vstupná, neuróny v~nej prijímajú vstupné informácie a~posielajú ich ďalej. Posledná vrstva neurónovej siete, ktorá poskytuje výsledok, sa nazýva výstupná. Medzi vstupnou a~výstupnou vrstvou sa môžu vyskytovať tzv.\ skryté vrstvy, ktoré slúžia na vykonávanie medzivýpočtov, čím umožňujú riešenie komplexnejších úloh. Výpočtový výkon siete je daný práve schopnosťou skrytých vrstiev modelovať nelineárne vzťahy medzi vstupmi a~výstupmi.\ \acrshort{uns} je možné rozdeliť do viacerých kategórií:
  \begin{enumerate}
    \item Podľa architektúry usporiadania neurónov do vrstiev:
    \begin{enumerate}
      \item \emph{Jednovrstvové siete}~--~\emph{perceptróny (\acrshort{slp})}~--~skladajú sa len zo vstupnej (nevykonávajúcej výpočty) a~výstupnej vrstvy; využíva sa úplné prepojenie, teda každý vstupný neurón je prepojený s~každým výstupným; obvykle využívajú skokové aktivačné funkcie a~ich účelom je klasifikácia do tried;
      \item \emph{Viacvrstvové siete}~--~\emph{viacvrstvové perceptróny (\acrshort{mlp})}~--~obsahujú aspoň jednu skrytú vrstvu; vstupná vrstva slúži iba na distribúciu vstupných informácií, výkonnú funkciu majú iba skryté vrstvy a~výstupná vrstva. Umožňujú riešenie lineárne neseparovateľných problémov.
      \item \emph{Hlboké neurónové siete}~--~majú vyšší počet skrytých vrstiev, umožňujú modelovanie komplexnejších závislostí. Medzi najznámejšie architektúry patria hlboké dopredné neurónové siete (\acrshort{dffn}), konvolučné neurónové siete (\acrshort{cnn}), používané v~oblasti spracovania obrazu, a~rekurentné neurónové siete (\acrshort{rnn}), vhodné na spracovanie sekvenčných dát~\cite{lecunDeepLearning2015,schmidhuberDeepLearningNeural2015}.
    \end{enumerate}
    \item Podľa spôsobu šírenia informácií v~rámci siete:
    \begin{enumerate}
      \item \emph{Dopredné siete}~--~(angl.\ \emph{feed-forward}) obsahujú iba väzby v~smere od vstupov k~výstupom,
      \item \emph{Rekurentné siete}~--~obsahujú aspoň jednu spätnú väzbu v~rámci tej istej vrstvy alebo v~rámci rôznych vrstiev,
    \end{enumerate}
  \end{enumerate}
  
  Kľúčom k~úspešnosti neurónových sietí vo vykonávaní učenej úlohy je proces ich učenia. Učenie je možné definovať ako proces zlepšovania výkonu na základe pozorovania okolitého prostredia~\cite{russellArtificialIntelligenceModern2010}. Proces učenia neurónovej siete zahŕňa úpravu váh $w_i$ na základe chyby medzi predikovaným a~skutočným výstupom. Najčastejšie sa používa algoritmus \emph{gradientného zostupu} v~kombinácii s~technikou \emph{spätného šírenia} (angl.\ \emph{backpropagation})~\cite{rumelhartLearningRepresentationsBackpropagating1986}. Cieľom je minimalizovať hodnotu funkcie odchýlky, napr.\ strednej kvadratickej chyby:
  \begin{equation}
  \mathcal{L} = \frac{1}{N} \sum_{i=1}^{N} {(y_i - \hat{y}_i)}^2,
  \end{equation}
  kde $y_i$ je skutočný a~$\hat{y}_i$ predikovaný výstup.
  
  % Prístupy k~učeniu \acrshort{uns}   je možné rozdeliť do niekoľkých kategórií:
  % \begin{enumerate}
  %   \item \emph{Učenie bez učiteľa}~--~vstupné údaje nenesú informáciu o~správnosti riešenia, neurónová sieť sa sama naučí nachádzať v~nich analogické znaky (zhlukovanie) alebo hľadá skryté súvislosti medzi skupinami objektov (asociácia);
  %   \item \emph{Učenie s~učiteľom}~--~učenie prebieha pomocou vzorových vstupno-výstupných dát, na základe ktorých sa neurónová sieť naučí funkciu mapujúcu zo vstupov na výstupy. Následne je schopná riešenie zovšeobecniť aj na neznáme vstupné údaje;
  %   \item \emph{Posilňované učenie (\acrshort{rl})}~--~v tomto prípade neprebieha učenie na predpripravených dátach; \acrshort{uns} je rovno nasadená do reálneho prostredia, v~ktorom sa učí za behu na základe určeného modelu systému (pravidlá jeho správania sa) a~kriteriálnej funkcie (ohodnocovací systém), pomocou ktorej určujeme, či, príp.\ do akej miery nám výsledok \acrshort{uns} vyhovuje. Neurónová sieť je postupne modifikovaná tak, aby sa čo najlepšie prispôsobila podmienkam prostredia;
  %   \item \emph{Neuro-evolúcia}~--~princíp je podobný ako pri \acrshort{rl}, s~tým rozdielom, že na hľadanie parametrov \acrshort{uns} sa využívajú evolučné algoritmy. Tie sú založené na generovaní veľkého množstva riešení (v tomto prípade parametrov \acrshort{uns}) v~početných iteráciách, pričom rôznymi metódami modifikácie jednotlivých riešení, generovania nových náhodných riešení a~výberu tých najvhodnejších pomocou zvolenej kriteriálnej funkcie dochádza k~postupnej konvergencii výsledného riešenia.
  % \end{enumerate}

  \subsection{Aktivačná funkcia}

  Aktivačná funkcia zabezpečuje nelinearitu neurónu, vďaka čomu je \acrshort{uns} schopná riešiť zložitejšie problémy. Výber vhodnej aktivačnej funkcie je jednou z~kritických častí návrhu \acrshort{uns} a~musí byť prispôsobený typu úlohy, ktorú má neurónová sieť vykonávať. V~pôvodnom modeli~\cite{mccullochLogicalCalculusIdeas1943} sa používala len binárna (skoková) funkcia $f(a) = \mathrm{sgn}(a)$. Tá je stále používaná v~perceptrónových neurónových sieťach, ktoré vykonávajú klasifikáciu. V~mnohých prípadoch sa však vyžaduje spojitý výstup siete, preto sa používajú viaceré typy spojitých aktivačných funkcií~\cite{aggarwalNeuralNetworksDeep2018}:
  \begin{itemize}
    \item \emph{lineárna funkcia}: $f(a) = a$~--~používa sa v~prípade, že výstupná premenná je reálne číslo; v~takom prípade rieši sieť regresiu najmenších štvorcov $\hat y = \sum^n_{i=1} w_i\, x_i$;
    
    \item \emph{sigmoida}: $f(a) = S(a) = \frac{1}{1+e^{-\beta\, a}}$~--~vhodná pre prípad odhadu pravdepodobnosti binárnej triedy, účelová funkcia má tvar $\hat y = - \log \left|\frac{y}{2} - 0,5 + \hat y \right|$ za predpokladu $y \in \{-1, 1\}$;

    \item \emph{hyperbolický tangens}: $f(a) = \tanh \left(\frac{a}{2}\right) = \frac{1 - e^{-a}}{1+e^{-a}} = 2\, S(2a) - 1$~--~ide v~podstate o~preškálovaný a~transformovaný sigmoid tak, že pripomína skokovú funkciu, avšak na rozdiel od nej je diferencovateľná. Je vhodná v~prípade, že výstup môže nadobúdať kladné aj záporné hodnoty. Navyše, jej gradient je viac koncentrovaný okolo nuly, vďaka čomu sa trénuje jednoduchšie než sigmoid. Tento efekt sa zvykne ešte zosilniť modifikáciami, ako napr.\ $f(a) = \tanh(3a)$;
    
    \item \emph{upravená lineárna jednotka (\acrshort{relu})}: $f(a) = \max\{a, 0\}$~--~patrí k~novším typom aktivačných funkcií; je výhodná pri trénovaní viacvrstvových sietí s~množstvom neurónov, a~to najmä vďaka rýchlejšiemu výpočtu gradientu, ale tiež pre nižšie riziko výskytu \emph{efektu miznúceho gradientu} a~zabránenie saturácii. Nevýhodou je strata informácie zo záporných vstupov a~riziko vzniku tzv.~\emph{mŕtveho neurónu}: keď sa raz váhy neurónu natrénujú na záporné hodnoty, jeho výstup bude vždy nulový;
    
    \item \emph{\uv{Hard Tanh}}: $f(a) = \max \{ \min [v,1],-1\}$~--~je to po častiach lineárna aproximácia hyperbolického tangensu; výpočtovo menej náročná, zachováva saturáciu, čo býva žiaduce napríklad pri výstupných neurónoch; podobne ako \acrshort{relu} má v~citlivej časti konštantný nenulový gradient, čo má význam pri spätnom šírení gradientu.
  \end{itemize}

  \begin{figure}[t!]
    \centering
    \begin{subfigure}{0.48\textwidth}
      \centering
      \input{img/activation/sgn.tex}
      \caption{skoková}\label{fig:sgn}
    \end{subfigure}
    \begin{subfigure}{0.48\textwidth}
      \centering
      \input{img/activation/lin.tex}
      \caption{lineárna}\label{fig:lin}
    \end{subfigure}
    \begin{subfigure}{0.48\textwidth}
      \centering
      \input{img/activation/sigmoid.tex}
      \caption{sigmoid}\label{fig:sigmoid}
    \end{subfigure}
    \begin{subfigure}{0.48\textwidth}
      \centering
      \input{img/activation/tanh.tex}
      \caption{hyperbolický tangens}\label{fig:tanh}
    \end{subfigure}
    \begin{subfigure}{0.48\textwidth}
      \centering
      \input{img/activation/relu.tex}
      \caption{ReLU}\label{fig:relu}
    \end{subfigure}
    \begin{subfigure}{0.48\textwidth}
      \centering
      \input{img/activation/htanh.tex}
      \caption{Hard Tanh}\label{fig:htanh}
    \end{subfigure}      
    \caption{Vybrané aktivačné funkcie.}\label{fig:activation}
  \end{figure}

  Aktivačné funkcie ReLU a~Hard Tanh v~poslednej dobe zväčša nahrádzajú klasické typy, ako sú sigmoid a~hyperbolický tangens, a~to pre zjednodušenie trénovania viacvrstvových neurónových sietí s~týmito funkciami~\cite{aggarwalNeuralNetworksDeep2018}. Grafické znázornenia aktivačných funkcií sa nachádzajú na obr.\ \ref{fig:activation}.

  V~\acrshort{mlp} sieťach sa v~neurónoch výstupnej vrstvy často využíva aj aktivačná funkcia \emph{softmax}. Jej úlohou je prepočítať výstupy poslednej skrytej vrstvy na pravdepodobnostné hodnoty, teda musí platiť $y_i \in \langle 0; 1 \rangle$ a~zároveň $\displaystyle\sum_i y_i = 1$. To je zabezpečené predpisom funkcie
  \begin{equation}
    f(y_i) = \frac{\exp (h_i)}{\displaystyle\sum_{j=1}^k \exp (h_j)}\ ;\ \forall i \in \{1,\dots,k\},
  \end{equation}
  kde $H = {(h_1,\dots,h_k)}^{\top}$ je vektor výstupov poslednej skrytej vrstvy a~$k$ je počet výstupných neurónov aj neurónov poslednej skrytej vrstvy. Tento typ aktivačnej funkcie je výhodné použiť na klasifikáciu väčšieho množstva tried~\cite{aggarwalNeuralNetworksDeep2018}.


\section{Perceptróny}\label{ssec:slp}

  Základným typom neurónových sietí sú perceptróny. Slúžia na klasifikáciu vstupných údajov a~pôvodne boli predstavené predstavené Rosenblattom v~roku 1958~\cite{rosenblattPerceptronProbabilisticModel1958}, ktorý ich architektúru rozdelil na tri vrstvy: senzorickú, asociačnú a~výstupnú. Senzorická vrstva sa namiesto plnohodnotných neurónov skladá len zo vstupných terminálov, ktorých počet zodpovedá rozmeru vstupného vektora a~ich úlohou je preposielanie vstupných hodnôt ďalej (príp.\ normalizácia vstupných hodnôt). Za ňou môže nasledovať asociačná, teda skrytá vrstva, v~ktorej neuróny slúžia na vykonávanie medzivýpočtov a~umožňujú riešenie lineárne neseparovateľných problémov (viac v časti~\ref{ssec:lin-sep}). Výstupy skrytej vrstvy nie sú čitateľné, slúžia ako vstupy pre neuróny poslednej, výstupnej vrstvy. V~pôvodnej architektúre váhy skrytej vrstvy nepodliehali učeniu. Napokon, výstupná vrstva slúži na výsledné výpočty siete. Počet neurónov v~nej zodpovedá počtu tried, ktoré sa v~rámci úlohy klasifikácie rozlišujú. Neuróny v~takejto sieti sú pospájané v~doprednom smere (angl.\ \emph{feed-forward}), teda zľava doprava. Všetky prepojenia perceptrónu sú jednosmerné, informácie sa posielajú len v~smere dopredu k~neurónom susediacim sprava. Nenachádzajú sa v~ňom žiadne spätné prepojenia ani prepojenia preskakujúce vrstvu~\cite{mehligMachineLearningNeural2021}.

  Počas nasledujúceho vývoju v~oblasti \acrshort{uns} však došlo z~didaktických dôvodov k~ustáleniu termínu \emph{perceptrón} iba na jednovrstvovú lineárne separovateľnú architektúru, \emph{jednovrstvový perceptrón (\acrshort{slp})}. Klasifikačné siete s~menej ako štyrmi skrytými vrstvami sa zvyknú označovať aj \emph{viacvrstvové perceptróny (\acrshort{mlp})}.
  
  \subsection{Jednovrstvové perceptróny}
  Vstupná vrstva jednovrstvového perceptrónu obsahuje $d$ vstupných terminálov pre vstupný vektor $X = {(x_1, x_2, \dots, x_d)}^{\top}$, ku ktorým je priradený vektor váh $W = {(w_1, w_2, \dots, w_d)}^{\top}$. Výstupná jednotka vypočíta vážený súčet vstupov a~výsledok spracuje pomocou skokovej funkcie, ktorá určuje výstupnú triedu~\cite{aggarwalNeuralNetworksDeep2018}:

  \begin{equation}
    \hat{y} = \mathrm{sign}(W \cdot X + \theta) = \mathrm{sign}\left(\sum_{j=1}^{d} w_j x_j + \theta \right),
  \end{equation}
  kde $\theta$ predstavuje tzv.\ \emph{bias}~--~prahovú hodnotu, ktorá umožňuje posun rozhodovacej hranice. V~implementáciách sa často reprezentuje ako špeciálny vstup s~hodnotou 1 a~vlastnou váhou.

  Perceptrón funguje ako lineárny klasifikátor: rozhodovaciu hranicu tvorí hyper-rovina \( W \cdot X + \theta = 0 \), ktorá rozdeľuje vstupný priestor na dve triedy. Tento prístup je účinný v~prípadoch, keď sú dáta \emph{lineárne separovateľné}.

  \subsection{Lineárna separovateľnosť~--~problém klasifikácie}\label{ssec:lin-sep}

  Problém lineárnej separovateľnosti je možné priblížiť na jednoduchom príklade. Majme klasifikačnú úlohu pre dvojrozmerné reálne vstupné vektory $X = {(x_1, x_2)}^{\top}$, ktoré majú byť rozdelené do cieľových skupín, teda tried $y \in \{-1, 1\}$. Na riešenie teoreticky stačí použiť jeden neurón so skokovou aktivačnou funkciou, teda výstup neurónu je počítaný ako skalárny súčin vstupov a~váh:
  \begin{equation}
    \hat y = \mathrm{sgn}(w_1 x_1 + w_2 x_2 - \theta) = \mathrm{sgn}(W \cdot X - \theta),
    \label{eq:lin-sep1}
  \end{equation}
  kde $W = {(w_1, w_2)}$ je vektor váh, ktoré pripájajú vstupy neurónu~\cite{mehligMachineLearningNeural2021}. Skalárny súčin, ktorý vystupuje vo vzťahu~\ref{eq:lin-sep1}, je možné riešiť aj geometricky po úprave:
  
  \begin{eqnarray}
    W &=& |W|
    \begin{bmatrix}
      \cos \alpha \\ \sin \alpha
    \end{bmatrix}
    \ ;\ |W| = \sqrt{w_1^2 + w_2^2}, \nonumber \\
    X &=& |X|
    \begin{bmatrix}
      \cos \beta \\ \sin \beta
    \end{bmatrix}
    \ ;\ |X| = \sqrt{x_1^2 + x_2^2}, \nonumber \\
    W \cdot X &=& |W||X| \cos (\alpha - \beta) = |W||X| \cos \varphi,
  \end{eqnarray}
  kde $\alpha$ a~$\beta$ sú uhly, ktoré zviera vektor váh, resp.\ vstupov s~osou $x_1$ a~$\varphi$ je uhol medzi nimi. Z~toho je zrejmé, že výsledok skalárneho súčinu bude kladný pre $\varphi \in (-\frac{\pi}{2}, \frac{\pi}{2})$, inak bude záporný. Dôsledkom toho je zabezpečená správna klasifikácia vtedy, keď je vektor váh kolmý na rozhodovaciu hranicu, ktorá vymedzuje jednotlivé triedy. Situácia je graficky znázornená na obr.~\ref{fig:classification-geometry}.

  \begin{figure}[b!]
    \centering
    \input{img/linear_separation/lin_sep_classification.tex}
    \caption{Geometrické riešenie klasifikácie.}\label{fig:classification-geometry}
  \end{figure}

  V~uvedenom príklade sme pre zjednodušenie neuvažovali s~biasom neurónu $\theta$. Jeho vplyv na rozhodovaciu hranicu je však zrejmý keď položíme argument zo vzťahu~\ref{eq:lin-sep1} rovný nule a~odvodíme
  \begin{equation}
    x_2 = -\frac{w_1}{w_2} x_1 + \frac{\theta}{w_2},
  \end{equation}
  z~čoho vyplýva, že bias má vplyv na priesečník rozhodovacej hranice s~osou $x_2$~\cite{mehligMachineLearningNeural2021}.

  Ak existuje taká lineárna hranica (napr.\ priamka v~2D, rovina v~3D, hyper-rovina vo vyšších dimenziách), ktorá dokáže oddeliť všetky vstupné prvky jednej triedy od druhej bez chyby, takúto množinu vstupov nazývame \emph{lineárne separovateľná} a~jej klasifikácia je zvládnuteľná pomocou jediného jediného neurónu.

  Dobrým príkladom na demonštráciu lineárnej separovateľnosti sú aj logické funkcie. Na obr.~\ref{fig:boolean} je porovnanie funkcií konjunkcie, ktorá je lineárne separovateľná a~exkluzívnej disjunkcie, ktorá nie je.

  \begin{figure}[t!]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}\centering
      \input{img/linear_separation/and.tex}
      \caption{Kojunkcia (AND)~--~lineárne separovateľná}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}\centering
      \input{img/linear_separation/xor.tex}
      \caption{Exkluzívna disjunkcia (XOR)~--~lineárne neseparovateľná}
    \end{subfigure}
    \caption[Lineárna separovateľnosť vybraných logických funkcií.]{Lineárna separovateľnosť vybraných logických funkcií.\ $\bullet{}$ pre pravd.\ hodnotu \uv{TRUE} ($y = 1$), $\circ{}$ pre pravd.\ hodnotu \uv{FALSE} ($y = -1$).}\label{fig:boolean}
  \end{figure}

  % Perceptrón sa teda skladá zo vstupných terminálov ${\bf x}^{(\mu)} = {\left(x_1^{(\mu)}, x_2^{(\mu)}, \dots, x_N^{(\mu)}\right)}^\top$, kde $N$ vyjadruje rozmer vstupných vektorov označených $\mu = 1,\dots,p$, $M$ výstupných neurónov podľa rozmeru riešenia úlohy (napr.\ počtu tried pri klasifikácii) a~určitého počtu skrytých vrstiev. V~prípade, že sieť neobsahuje skryté vrstvy, ide o~jednovrstvovú neurónovú sieť; ak obsahuje aspoň jednu skrytú vrstvu, je to viac-vrstvová neurónová sieť. Výstupy skrytých neurónov $V_j$ a~výstupných neurónov $O_i$ sú potom počítané analogicky ako
  % \begin{subequations}
  %   \begin{align}
  %     V_j &= g\left(\sum_k w_{jk} x_k - \theta_j \right), \\
  %     O_i &= g\left(\sum_j W_{ij} V_j - \Theta_i \right),
  %   \end{align}
  % \end{subequations}
  % kde $w_{jk}, W_{ij}$ sú váhy prepojení medzi jednotlivými vrstvami neurónov a~$\theta_j, \Theta_i$ sú prahy citlivosti skrytých, resp.\ výstupných neurónov~\cite{mehligMachineLearningNeural2021}.

  % Jednovrstvové perceptróny dokážu riešiť len lineárne separovateľné problémy. Pre ne musí platiť $p \leq N$. Pridanie skrytej vrstvy umožňuje riešiť aj klasifikačné problémy, ktoré nie sú lineárne separovateľné a~ktorých je väčšina. Rozdiel medzi týmito dvomi základnými typmi problémov na príklade dvojrozmerného vstupného priestoru je na obr.~\ref{fig:lin-sep}. Perceptróny sa používajú na riešenie problému triedenia (klasifikácie). Ten je definovaný pomocou trénovacej množiny vstupov ${\bf x}^{(\mu)}$ a~zodpovedajúcich vektorov cieľových výstupov ${\bf t}^{(\mu)} = {\left(t_1^{(\mu)}, t_2^{(\mu)}, \dots, t_M^{(\mu)}\right)}^\top$. Úlohou je nájsť také hodnoty váh a~prahov, aby platilo 
  % \begin{equation}
  %   \forall i, \mu;\ O_i^{(\mu)} = t_i^{(\mu)}.
  %   \label{eq:classification}
  % \end{equation}
  % Existuje viacero metód a~algoritmov, ktoré riešia túto úlohu. Pri jednovrstvových perceptrónoch je najzákladnejším algoritmus iteratívneho učenia, v~ktorom je hodnota váh modifikovaná podľa Rosenblattovho pravidla~\cite{rosenblattPerceptronProbabilisticModel1958}:
  % \begin{equation}
  %   w'_{ij} = w_{ij} + \delta w^{(\mu)}_{ij};\ \delta w^{(\mu)}_{ij} = \eta t^{(\mu)}_i x^{(\mu)}_j,
  % \end{equation}
  % kde $\eta > 0$ je faktor učenia. Tento vzťah je aplikovaný iteratívne na množinu trénovacích vstupov, až kým nie je splnená podmienka~\ref{eq:classification}.
  

  % \begin{figure}[b!]
  %   \centering
  %   \includegraphics[width=0.6\textwidth]{img/linear_separation/lin_sep_compare.png}
  %   \caption{Lineárne separovateľná a~neseparovateľná klasifikácia~\cite{mehligMachineLearningNeural2021}}\label{fig:lin-sep}
  % \end{figure}    

\subsection{Viacvrstvové perceptróny}\label{ssec:mlp}

  V~prípade \acrshort{slp} je jediná výpočtová vrstva výstupná, keďže vstupná vrstva nevykonáva žiadne výpočty. Viacvrstvové perceptróny (\acrshort{mlp}) obsahujú viacero výpočtových vrstiev. Keďže ale výsledky vrstiev pridaných medzi vstupnú a~výstupnú vrstvu nie sú viditeľné pre užívateľa, označujú sa ako \emph{skryté vrstvy}.
  
  Pridanie skrytej vrstvy umožňuje \acrshort{mlp} riešiť lineárne neseparovateľné problémy, nakoľko rozdeľuje klasifikačný priestor na viac než len dva segmenty. Jednoduchý príklad takejto siete, ktorá klasifikuje dvojrozmerný vstupný vektor pomocou troch skrytých a~jedného výstupného neurónu, môžeme vidieť na obr.~\ref{fig:mlp}. Neuróny vypočítavajú výstupné hodnoty pomocou skokovej aktivačnej funkcie v~tvare
  \begin{subequations}
    \begin{align}
      h_j &= f\left(\sum_k w_{jk} x_k - \theta_j \right), \\
      \hat y_i &= f\left(\sum_j W_{ij} h_j - \theta_i \right),
    \end{align}
  \end{subequations}
  pričom výsledok môže nadobúdať hodnoty $y_i = \{-1, +1\}$~\cite{mehligMachineLearningNeural2021}. Každý z~neurónov v~skrytej vrstve má vlastnú rozhodovaciu hranicu. Cieľom je teda nájsť také hodnoty $w_{jk}$ a~$\theta_j$, aby rozhodovacie hranice rozdelili vstupnú rovinu (keďže máme dvojrozmerný vstup, môžeme si ho predstaviť ako súradnice bodov v~rovine) na také segmenty, ktoré budú obsahovať len prvky jednej triedy. Situácia je znázornená na obr.~\ref{fig:lin-sep-solution}.
  \begin{figure}[b!]
    \centering
    \includegraphics[width=0.4\textwidth]{img/perceptron.tex}
    \caption[Viacvrstvový perceptrón.]{Viacvrstvový perceptrón s~jednou skrytou vrstvou so vstupnými terminálmi $x_k$, skrytými neurónmi $h_{j}$, výstupným neurónom $y_1$, a~váhami prepojení medzi vrstvami $w_{jk}$ a~$w_{1j}$.~\cite{mehligMachineLearningNeural2021}}\label{fig:mlp}
  \end{figure}

  \begin{figure}[t!]
    \centering
    \begin{subfigure}[t]{0.48\textwidth}\centering
      \input{img/linear_separation/lin_sep.tex}
      \caption{Lineárne separovateľný problém klasifikovaný pomocou \acrshort{slp}.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[t]{0.48\textwidth}\centering
      \input{img/linear_separation/lin_unsep.tex}
      \caption{Lineárne neseparovateľný problém klasifikovaný pomocou \acrshort{mlp}, tvoriacej po častiach lineárnu rozhodovaciu hranicu.}
    \end{subfigure}
    \caption{Porovnanie klasifikácie pomocou \acrshort{slp} a~\acrshort{mlp}.}\label{fig:lin-sep-solution}
  \end{figure}

  Mechanizmus klasifikácie je možné si predstaviť tak, že každý zo vstupných bodov obdrží množinu výstupných hodnôt skrytých neurónov $\{h_1, h_2, h_3\}$. Každý zo segmentov je potom charakterizovaný unikátnou kombináciou výstupov skrytej vrstvy. Váhy $w_{1j}$ a~prahové hodnoty ${\theta_j}$ musia byť nastavené tak, aby každému vzniknutému segmentu vstupnej roviny pridelil výstupný neurón správnu triedu $y_i$, čo už opäť predstavuje lineárne separovateľný problém. To prebieha v~tomto konkrétnom príklade na základe vzťahu
  \begin{equation}
    \hat y_1 = \mathrm{sgn} \left( h_1 + h_2 + h_3 \right).
  \end{equation}
  Je potrebné si uvedomiť, že neexistuje jedinečné riešenie takéhoto problému; sú možné viaceré kombinácie váh a~prahových hodnôt neurónov, ktoré by spĺňali požiadavky klasifikácie, ako aj viaceré rozloženia siete. Vo všeobecnosti však platí, že pre zložitejšie problémy klasifikácie je potrebných viac skrytých neurónov~\cite{mehligMachineLearningNeural2021}. V~práci autorov Minsky a~Papert z~roku 1969~\cite{minskyPerceptronsIntroductionComputational2017} bolo preukázané, že všetky logické operácie je možné vyriešiť pomocou \acrshort{mlp} za podmienky, že aspoň jeden zo skrytých neurónov je pripojený ku všetkým vstupným terminálom.

  Pri základnej architektúre dopredných sietí sa predpokladá, že všetky neuróny jednej vrstvy sú pripojené ku všetkým neurónom nasledujúcej vrstvy\@. V~prípade, že sieť obsahuje len jednu skrytú vrstvu, hovoríme o~tzv.\ \emph{plytkej sieti}\@. \acrshort{mlp} však môže obsahovať viac než len jednu skrytú vrstvu. Je matematicky dokázané, že \acrshort{uns} s~tromi vrstvami sú schopné aproximovať ľubovoľnú spojitú nelineárnu funkciu a~siete so štyrmi vrstvami stačia na aproximáciu ľubovoľnej nespojitej nelineárnej funkcie. Ak sieť obsahuje viac ako tri skryté vrstvy, zvykne sa nazývať \emph{hlboká (dopredná) sieť (\acrshort{dffn})}.
  
  Pre praktický zápis matematických operácií vnútri siete sa na označenie jednotlivých prvkov používa vektorová, resp.\ maticová forma.\ \acrshort{mlp} obsahuje vstupnú vrstvu označenú vektorom $X$ veľkosti $d \times 1$, kde $d$ je rozmer vstupných dát, $k$ skrytých vrstiev o~veľkosti $p_1 \dots p_k$, ktorých výstupy označujú vektory $H_1 \dots H_k$. Vstupná vrstva je na prvú skrytú vrstvu pripojená váhami označenými maticou $W_1$ veľkosti $d \times p_1$, zatiaľ čo $r$-tá skrytá vrstva je prepojená s~($r+1$)-ou maticou $W_r$ veľkosti $p_r \times p_{r+1}$. Za predpokladu, že máme vektor výstupných neurónov $O = {(o_1,\dots,o_m)}^{\top}$, tie sa pripájajú k~poslednej skrytej vrstve pomocou váh označených maticou $W_{k+1}$ s~veľkosťou $p_k \times m$. Vstupno-výstupný výpočet \acrshort{mlp} je potom možné zapísať pomocou nasledujúcich rekurzívnych vzťahov~\cite{aggarwalNeuralNetworksDeep2018}:
  \begin{subequations}
    \begin{align}
      H_1 &= f \left( W_1^{\top} X \right) \\
      H_{p+1} &= f \left( W_{p + 1}^{\top} H_p \right)\ \forall p \in (1 \dots k~- 1) \\
      o~&= f \left( W_{k+1}^{\top} H_k \right).
    \end{align}
  \end{subequations}
  
  Základnými parametrami pri návrhu siete sú teda počet skrytých vrstiev, počty skrytých vrstiev a~neurónov v~jednotlivých vrstvách, a~tiež výber aktivačných funkcií neurónov. V~neposlednom rade, úspešnosť trénovania neurónovej siete závisí od výberu optimalizačnej funkcie odchýlky.
  
\section{Trénovanie UNS}\label{ssec:training}

  Trénovanie umelých neurónových sietí predstavuje kľúčový proces, prostredníctvom ktorého sa model učí z~dostupných dát optimalizáciou svojich parametrov, najmä váh a~prahov neurónov. Cieľom trénovania je minimalizovať odchýlku medzi výstupom siete $\hat y$ a~požadovaným výstupom $y$, čo sa dosahuje iteratívnym prispôsobovaním parametrov na základe výpočtu gradientu chyby. Jedným z~kľúčových aspektov trénovania je preto výber vhodnej funkcie na výpočet odchýlky (v angl.\ literatúre označovanej \emph{loss function}) vzhľadom ku konkrétnemu typu úlohy, ktorú \acrshort{uns} rieši. Obrázok~\ref{fig:training} znázorňuje základný princíp trénovania \acrshort{uns}. V~tejto časti sa podrobnejšie pozrieme na jeden z~prvých používaných algoritmov~--~perceptrónové kritérium~-- ale aj na v~súčasnosti najbežnejšiu metódu spätného šírenia chyby. Priblížime tiež časté problémy, ktoré sa pri procese trénovania môžu vyskytnúť.

  \begin{figure}[b!]
    \centering
    \input{img/training.tex}
    \caption{Základná schéma algoritmov trénovania UNS.}\label{fig:training}
  \end{figure}

  \subsection{Perceptrónové kritérium}
    Pôvodný perceptrónový algoritmus prebiehal heuristicky pomocou elektrických obvodov, na rozdiel od súčasného chápania problému ako optimalizačnej úlohy. V~každom prípade, cieľ ostáva zachovaný a~je ním nájsť také váhy $W$, ktoré minimalizujú počet chybne klasifikovaných príkladov v~množine trénovacích vstupno-výstupných párov $(X, y)$. Úlohu perceptrónového algoritmu je teda možné zapísať v~tvare účelovej funkcie najmenších štvorcov pre všetky trénovacie objekty datasetu $\mathcal{D}$~\cite{aggarwalNeuralNetworksDeep2018} ako
    \begin{equation}
      \mathcal{L} = \arg\min_{W} \sum_{(X, y) \in \mathcal{D}} {\left( y - \hat y \right)}^2 = \sum_{(X, y) \in \mathcal{D}} {\left[ y - \operatorname{sign}(W \cdot X) \right]}^2
    \end{equation}

    Tento typ minimalizácie účelovej funkcie sa označuje aj ako \emph{funkcia odchýlky} (z~angl.\ \emph{loss function})~\cite{aggarwalNeuralNetworksDeep2018}. Problém takto formulovanej funkcie však spočíva v~použití skokovej funkcie signum, ktorá nie je diferencovateľná, obsahuje skoky a~na veľkej časti definičného oboru má konštantné hodnoty, čo spôsobuje nulový gradient, teda nie je vhodná pre metódu gradientného zostupu. Na odvodenie jej hladkej náhrady sa používa tzv.\ \emph{perceptrónové kritérium}. Jeho princíp spočíva v~prvotnom definovaní účelovej funkcie s~výstupom 0/1 pre jeden príklad vstupno-výstupného páru $(X, y)$~\cite{aggarwalNeuralNetworksDeep2018}:

    \begin{equation}
      L^{(0/1)}(X, y) = \frac{1}{2}\,{(y - \hat{y})}^2 = 1 - y \cdot \mathrm{sign}(W \cdot X + \theta).
    \end{equation}

    Táto funkcia nie je diferencovateľná kvôli prítomnosti skokovej funkcie $\mathrm{sign}$, preto sa na analytické účely nahrádza hladkou účelovou funkciou~\cite{aggarwalNeuralNetworksDeep2018}:

    \begin{equation}
      L(X, y) = \max\left\{ -y\,(W \cdot X + \theta),\, 0 \right\}.
    \end{equation}

    Táto tzv.\ \emph{hladká náhrada funkcie odchýlky} penalizuje len nesprávne klasifikované príklady a~jej gradient možno využiť na aktualizáciu váh~\cite{aggarwalNeuralNetworksDeep2018}:

    \begin{equation}
      W \:\leftarrow\: W + \alpha\, (y - \hat{y})\, X,
    \end{equation}
    kde $\alpha$ je rýchlosť učenia. Váhy sa menia iba v~prípade, že $y \ne \hat{y}$. Tento prístup je stochastickým gradientným zostupom (\acrshort{sgd}), ktorý optimalizuje (aj keď len približne) uvedenú hladkú funkciu.

  % \subsubsection{Gradientný zostup pre lineárne jednotky}

  %   v~predošlej časti sme ukázali využitie metódy gradientného zostupu na trénovanie perceptrónu so skokovou aktivačnou funkciou, ktorá musí byť v~procese učenia zamenená za hladkú náhradu. Alternatívou k~takémuto postupu je rovno nahradenie skokovej aktivačnej funkcie inou, diferencovateľnou funkciou. Najjednoduchšou možnosťou je lineárna funkcia. Takýto neurón sa potom nazýva \emph{lineárna jednotka} (z angl.\ \emph{linear unit})~\cite{mehligMachineLearningNeural2021}. Za predpokladu, že $\theta = 0$ je výstup siete počítaný ako
  %   \begin{equation}
  %     \hat y_i = \sum_k w_{ik} x_k.
  %   \end{equation}
    
  %   Pre lineárne separovateľné problémy je možné definovať účelovú funkciu
  %   \begin{equation}
  %     \mathcal{L} = \frac{1}{2} \sum_{i\mu} {\left( y_i^{(\mu)} - \hat y_i^{(\mu)} \right)}^2,
  %   \end{equation}
  %   ktorej vstupom sú hodnoty váh a~globálne minimum dosahuje keď je splnený vzťah~\ref{eq:classification}. Trénovanie prebieha postupným modifikovaním hodnôt váh pomocou gradientného zostupu~\cite{mehligMachineLearningNeural2021}:
  %   \begin{equation}
  %      W \:\leftarrow\: W + \delta w_{mn}^{(\mu)};\ \delta w_{mn}^{(\mu)} = -\alpha \frac{\partial\, \mathcal{L}}{\partial\, w_{mn}} = \alpha \sum_{\mu} {\left( y^{(\mu)}_m - \hat y^{(\mu)}_m \right)}x^{(\mu)}_n.
  %   \end{equation} 


  \subsection{Spätné šírenie chyby}

    Optimalizačné kritérium pre \acrshort{slp} je ľahko definovateľné, keďže výstup siete je priamou funkciou váh. To umožňuje jednoduchý výpočet gradientu pomocou diferenciácie. Pri viacvrstvových sieťach je však odchýlka výstupu komplikovanou zloženou funkciou váh v~predošlých vrstvách, ktorú môžeme zjednodušene zapísať ako $\mathcal{L} = f(o(h_r(w_r)))$. Preto sa za účelom výpočtu jej gradientu používa algoritmus \emph{spätného šírenia chyby}. Ten využíva reťazové pravidlo diferenciálneho počtu, ktoré počíta gradienty chyby ako sumu násobkov lokálnych gradientov skrytých neurónov pozdĺž rôznych ciest od daného neurónu až po výstup. Keďže počet ciest medzi skrytým neurónom a~výstupom môže rásť exponenciálne s~hĺbkou a~šírkou siete, priamy výpočet všetkých príspevkov by bol výpočtovo náročný. Preto sa spätné šírenie realizuje efektívne pomocou dynamického programovania, ktoré opakovane (rekurzívne) využíva už vypočítané medzivýsledky. Výpočet spätného šírenia chyby prebieha vo viacerých cykloch, tzv.\ \emph{epochách}, pričom každá pozostáva z~dvoch hlavných fáz:
    \begin{enumerate}
      \item \emph{Dopredná}: Cez sieť sa v~smere od vstupu k~výstupu postupne prepočítavajú aktivácie všetkých neurónov pre každú trénovaciu vzorku na základe aktuálneho nastavenia váh. Výstup siete je porovnaný s~požadovaným výstupom trénovacích dát, na základe čoho sa vypočíta odchýlka výstupu.
      \item V~každej vrstve v~smere od výstupu k~vstupu sa vypočíta lokálny gradient odchýlky voči daným váham a~aktiváciám z~predchádzajúcej vrstvy, na základe čoho sú prepočítané jednotlivé hodnoty váh.
    \end{enumerate}

    Uvažujme sekvenciu skrytých neurónov $h_1 \dots h_k$, ktorá je zakončená výstupným neurónom $o$. Keď budeme uvažovať len jedinú cestu z~neurónu $h_1$ do $o$, potom je možné gradient váh smerujúcich do $r$-tého neurónu, ktoré označíme $w_{(h_{r-1},h_r)}$, vypočítať pomocou reťazového pravidla jednej premennej~\cite{aggarwalNeuralNetworksDeep2018}:
    \begin{equation}
      \frac{\partial\, \mathcal{L}}{\partial\, w_{(h_{r-1},h_r)}} = \frac{\partial\, \mathcal{L}}{\partial\, o} \cdot
        \left[ \frac{\partial\, o}{\partial\, h_k} \prod_{i=r}^{k-1} \frac{\partial\, h_{i+1}}{\partial\, h_i}
        \right] \cdot
        \frac{\partial\, h_r}{\partial\, w_{(h_{r-1},h_r)}} 
        \ \forall r \in 1 \dots k
        \label{eq:chain-rule-single}
    \end{equation}

    Vo všeobecnosti však bude v~\acrshort{mlp} sieti existovať exponenciálne väčšia množina prepojení neurónov $h_r$ a~$o$, ktorú označíme $\mathcal{P}$. Vzťah~\ref{eq:chain-rule-single} je teda možné zovšeobecniť pre všetky existujúce prepojenia pomocou reťazového pravidla viacerých premenných:
    \begin{equation}
      \frac{\partial\, \mathcal{L}}{\partial\, w_{(h_{r-1},h_r)}} = 
      \underbrace{
        \frac{\partial\, \mathcal{L}}{\partial\, o} \cdot
          \left[ \sum_{[h_r, h_{r+1},\dots,h_k,o]\in \mathcal{P}} 
            \frac{\partial\, o}{\partial\, h_k} \prod_{i=r}^{k-1} \frac{\partial\, h_{i+1}}{\partial\, h_i}
        \right] }_{\Delta(h_r, o) = \frac{\partial\, \mathcal{L}}{\partial\, h_r}} \cdot
      \frac{\partial\, h_r}{\partial\, w_{(h_{r-1},h_r)}}
      \label{eq:chain-rule-multi}
    \end{equation}
    Tento vzťah je logicky rozdelený na dve časti. Prvá časť, označená ako $\Delta(h_r, o) = \frac{\partial\, \mathcal{L}}{\partial\, h_r}$, predstavuje agregovaný gradient chyby vzhľadom na výstup neurónu $h_r$ a~zahŕňa vplyv všetkých ciest vedúcich z~tohto neurónu až po výstup $o$. Nakoľko jeho veľkosť exponenciálne narastá s~veľkosťou siete, jeho výpočet je komplikovanejší. Posledný činiteľ vzťahu predstavuje vplyv zmeny váh vstupujúcich do skrytého neurónu $h_r$ na jeho výstup. Je možné ho vypočítať pomocou derivácie aktivačnej funkcie ako
    \begin{equation}
      \frac{\partial\, h_r}{\partial\, w_{(h_{r-1},h_r)}} = h_{r-1} \cdot f'(a_{h_r})
    \end{equation} 
    
    Pri výpočte už spomenutého agregovaného člena $\Delta (h_r,o)$ sa využíva fakt, že výpočtový graf siete je acyklický, preto je možné začať výpočet spätných gradientov od výstupnej vrstvy. Najskôr sa vypočíta $\Delta (h_k,o)$ pre poslednú skrytú vrstvu $h_k$, a~následne sa gradienty šíria späť smerom k~vstupným vrstvám pomocou rekurzívne definovaného vzťahu. Navyše, každý výstupný neurón je pri výpočte inicializovaný na hodnotu $\frac{\partial\, \mathcal{L}}{\partial\, o} = \Delta (o,o)$. Rekurzívny vzťah pre $\Delta (h_r,o)$ sa následne pomocou reťazového pravidla odvodí ako
    \begin{equation}
      \Delta (h_r,o) = \frac{\partial\, \mathcal{L}}{\partial\, h_r} = \sum_{h:h_r \:\rightarrow\: h} \frac{\partial\, \mathcal{L}}{\partial\, h} \frac{\partial\, h}{\partial\, h_r} = \sum_{h:h_r \:\rightarrow\: h} \frac{\partial\, h}{\partial\, h_r} \Delta (h,o),
      \label{eq:back-prop1}
    \end{equation}
    kde $\Delta (h,o)$ je gradient predchádzajúcej vrstvy (bližšie k~výstupu), teda je potrebné odvodiť parciálnu deriváciu $\frac{\partial\, h}{\partial\, h_r}$. Na to sa využije skutočnosť, že skryté neuróny $h_r$ a~$h$ sú spojené váhou $w_{(h_r,h)}$ a~$a_h$ je argument aktivačnej funkcie v~neuróne $h$. Teda pomocou reťazového pravidla jednej premennej je možné odvodiť:
    \begin{equation}
      \frac{\partial\, h}{\partial\, h_r} = \frac{\partial\, h}{\partial\, a_h} \cdot \frac{\partial\, a_h}{\partial\, h_r} = \frac{\partial\, f(a_h)}{\partial\, a_h} \cdot w_{(h_r, h)} = f'(a_h) \cdot w_{(h_r, h)}
      \label{eq:back-prop2}
    \end{equation}
    Dosadením vzťahu~\ref{eq:back-prop2} do~\ref{eq:back-prop1} získavame konečný rekurzívny vzťah pre výpočet agregovaného gradientu chyby vzhľadom na aktiváciu neurónu $h_r$, ktorý umožňuje efektívny výpočet gradientov bez opakovaného prechodu rovnakých ciest v~sieti.~\cite{aggarwalNeuralNetworksDeep2018}:
    \begin{equation}
      \Delta (h_r,o) = \sum_{h:h_r \:\rightarrow\: h} f'(a_h) \cdot w_{(h_r,h)} \cdot \Delta (h,o).
      \label{eq:back-prop3}
    \end{equation}

    Rekurzívny vzťah dynamického programovania zo vzťahu~\ref{eq:back-prop3} je v~skutočnosti možné vypočítať viacerými spôsobmi, v~závislosti od toho, ktoré premenné sa použijú ako medzičlánky v~reťazovom pravidle. Všetky tieto varianty výpočtu sú ekvivalentné z~hľadiska výsledného gradientu získaného spätným šírením chyby. Vo vzťahu~\ref{eq:chain-rule-multi} sa ako premenné medzičlánku reťazového pravidla používajú výstupy skrytých vrstiev $h$. V~literatúre sa však často uvádza alternatívna verzia, kedy sa namiesto nich použijú argumenty aktivačných funkcií neurónov $a_h$, teda hodnoty vypočítané po lineárnej transformácii, ale ešte pred aplikáciou aktivačnej funkcie. Preto je namiesto rovnice~\ref{eq:chain-rule-multi} možné použiť aj nasledovný variant reťazového pravidla~\cite{aggarwalNeuralNetworksDeep2018}:
    \begin{equation}
      \frac{\partial\, \mathcal{L}}{\partial\, w_{(h_{r-1},h_r)}} = 
      \underbrace{
        \frac{\partial\, \mathcal{L}}{\partial\, o} \cdot f'(a_o) \cdot
          \left[ \sum_{[h_r, h_{r+1},\dots,h_k,o]\in \mathcal{P}} 
            \frac{\partial\, a_o}{\partial\, a_{h_k}} \prod_{i=r}^{k-1} \frac{\partial\, a_{h_{i+1}}}{\partial\, a_{h_i}}
        \right] }_{\delta(h_r, o) = \frac{\partial\, \mathcal{L}}{\partial\, a_{h_r}}} \cdot
      \underbrace{
        \frac{\partial\, a_{h_r}}{\partial\, w_{(h_{r-1},h_r)}}.
      }_{h_{r-1}}
      \label{eq:chain-rule-multi2}
    \end{equation}
    v~ňom je namiesto predošlej \uv{medzipremennej} $\Delta (h_r,o) = \frac{\partial\, \mathcal{L}}{\partial\, h_r}$ použitá $\delta (h_r,o) = \frac{\partial\, \mathcal{L}}{\partial\, a_{h_r}}$. Inicializácia počiatočného stupňa rekurzie je daná ako
    \begin{equation}
      \delta (o,o) = \frac{\partial\, \mathcal{L}}{\partial\, a_o} = f'(a_o) \cdot \frac{\partial\, \mathcal{L}}{\partial\, o}.
    \end{equation}

    Následne je možné opäť použiť reťazové pravidlo viacerých premenných na zostavenie vzťahu pre rekurziu agregovaného gradientu chyby~\cite{aggarwalNeuralNetworksDeep2018}:
    \begin{equation}
      \delta (h_r,o) = \frac{\partial\, \mathcal{L}}{\partial\, a_{h_r}} 
                     = \sum_{h:h_r \:\rightarrow\: h} 
                     \overbrace{
                      \frac{\partial\, \mathcal{L}}{\partial\, a_h}
                     }^{\delta (h,o)}
                     \underbrace{
                      \frac{\partial\, a_h}{\partial\, a_{h_r}}
                     }_{f'(a_{h_r}) w(h_r,h)}
                     = f'(a_{h_r}) \sum_{h:h_r \:\rightarrow\: h} w_{(h_r,h)} \cdot \delta(h,o)
    \end{equation}
    Výsledný gradient účelovej funkcie odchýlky siete je potom vyjadrený nasledujúcim vzťahom:
    \begin{equation}
      \frac{\mathcal{L}}{\partial\, w_{(h_{r-1},h_r)}} = \delta (h_r,o) \cdot h_{r-1}
    \end{equation}

    Proces trénovania \acrshort{uns} môže prebiehať v~dvoch režimoch. Pri \emph{vzorkovom režime} dochádza k~úprave váh po privedení každej novej vstupnej vzorky. V~\emph{dávkovom režime} sa váhy korigujú až po uskutočnení celej epochy, teda po kompletnom prechode celého trénovacieho súboru cez sieť, s~využitím globálnej chyby zo všetkých vzoriek a~výstupov.
        
    \subsection{Problémy trénovania UNS}\label{ssec:training-problems}

      Neurónové siete poskytujú silný nástroj pri riešení komplexných a~analyticky ťažko odvoditeľných úloh. Aby ich trénovanie nebolo kontraproduktívne, je treba si uvedomiť problémy, ktoré s~ním môžu byť spojené. Medzi najzávažnejšie problémy patria \emph{pretrénovanie} a~\emph{problém miznúceho gradientu}. Ťažkosti môžu nastať aj pri nedostatočnej konvergencii účelovej funkcie, uviaznutí v~lokálnom alebo falošnom optime či nedostatočnom výpočtovom výkone.

      \subsubsection{Pretrénovanie} \

      Podstata trénovania neurónových sietí, ktoré je príkladom \emph{učenia s~učiteľom}, spočíva v~tom, že sieť je schopná sa na základe testovacích dát naučiť všeobecné závislosti medzi vstupnými a~výstupnými hodnotami, ktoré má byť následne schopná rozlíšiť aj na neznámych dátach. Po ukončení trénovania siete teda vždy prebieha fáza testovania, kedy je na vstup siete privedená množina vzoriek, ktoré sú odlišné od trénovacích.

      Pretrénovanie (angl.\ \emph{overfitting}) je jav, ktorý môže nastať dôsledkom neskorého ukončenia procesu trénovania, nedostatočnej trénovacej množiny dát, či nevhodnej architektúry siete. Prejavuje sa tak, že neurónová sieť síce dosahuje vysokú úspešnosť pri trénovacích dátach, no jej úspešnosť výrazne klesá pri zovšeobecnení na testovacích dátach, ktoré sú odlišné. Tento problém nastáva často napríklad vtedy, keď \acrshort{uns} obsahuje viac neurónov a~prepojení, než zodpovedá skutočnej zložitosti riešenej úlohy, či v~prípade nedostatočne veľkej testovacej množiny, čo do počtu a~rozsahu prvkov vstupných vzoriek. V~prvom prípade je dôsledkom, že sieť sa naučí zaznamenať aj také súvislosti vo vstupných dátach, ktoré nie sú všeobecné a~súvisia len s~danou množinou vstupných dát. V~druhom prípade je zase problém v~tom, že sieť sa nestihne naučiť všetky vstupno-výstupné závislosti.

      Existuje viacero prístupov, ktoré slúžia na minimalizáciu rizika vzniku pretrénovania:
      \begin{enumerate}
        \item \emph{regularizácia}~--~obmedzuje zložitosť modelu (siete). Do účelovej funkcie je pridaný penalizačný člen, ktorý uprednostňuje menšie hodnoty váh. Dochádza tak k~\uv{preriedeniu siete}, keďže obsahuje menšie množstvo významnejších prepojení (s hodnotou ďalej od nuly). Je možné ju interpretovať ako formu biologicky motivovaného \uv{zabúdania} nepodstatných vzorov.
        \item \emph{prispôsobenie architektúry siete}~--~využíva známe súvislosti vo vstupných dátach. Keď pri návrhu siete berieme do úvahy osobitosti úlohy, ktorú chceme riešiť, je možné zapracovať do architektúry siete také súvislosti, o~ktorých vieme, že sa vyskytujú vo vstupných dátach (napr.\ súvislosť susedných pixelov pri spracovaní obrazu alebo súvislosť nasledujúcich slov pri spracovaní textu). To umožňuje použiť tie isté sady parametrov na spracovanie viacerých častí vstupu, čím sa znižuje počet neznámych parametrov a~zjednodušuje model. Príkladom takýchto sietí sú \emph{rekurentné} alebo \emph{konvolučné} n.\ s.
        \item \emph{krížová validácia} (angl.\ \emph{cross validation})~--~zabraňuje sieti naučiť sa nevšeobecné znaky. Zo súboru testovacích vstupov je vyčlenený validačný súbor. Učenie prebieha na zvyšných trénovacích dátach, ale účelová funkcia sa paralelne vyhodnocuje aj na validačných dátach. V~momente, keď hodnota validačnej účelovej funkcie začne stagnovať alebo dokonca stúpať~--~čo indikuje, že sieť sa prestala učiť všeobecné znaky a~začína sa učiť znaky špecifické pre trénovacie dáta~--~učenie je zastavené (často už po niekoľkých epochách).
        \item \emph{výmena šírky za hĺbku}~--~namiesto veľkého počtu neurónov v~jednej vrstve a~malému počtu skrytých vrstiev sa preferuje málo neurónov a~veľa skrytých vrstiev. Takáto \emph{hlboká sieť} je schopná dosiahnuť rovnakú, a~často aj lepšiu úspešnosť, a~navyše môže obsahovať menšie množstvo parametrov.
      \end{enumerate}

      \subsubsection{Miznúce a~explodújúce gradienty}

        Problém miznúcich a~explodujúcich gradientov býva zvyčajne spojený s~trénovaním hlbokých neurónových sietí a~súvisí s~využívaním reťazového pravidla (vzťah~\ref{eq:chain-rule-multi}) pri spätnom šírení chyby. Keďže pri výpočte celkového gradientu účelovej funkcie dochádza k~početnému násobeniu lokálnych gradientov pozdĺž prepojení, zvlášť pri veľkom počte skrytých vrstiev môže spôsobiť buď exponenciálny útlm k~nule alebo naopak rýchly nárast gradientu. Oba javy tak spôsobujú nestabilitu procesu trénovania.

        Častou príčinou vzniku miznúceho gradientu býva napríklad použitie aktivačnej funkcie sigmoid. Tá má v~celom definičnom obore deriváciu menšiu ako 0,25. Riešením je jej náhrada funkciou \acrshort{relu}. Okrem tohto riešenia existujú aj pokročilejšie prístupy, ako napríklad~\cite{aggarwalNeuralNetworksDeep2018}:
        \begin{enumerate}
          \item \emph{Adaptívne rýchlosti učenia}~--~namiesto jednej globálnej rýchlosti učenia $\alpha$ pre celú sieť sa dynamicky prispôsobuje rýchlosť učenia každého parametra na základe historických gradientov. To zvyšuje efektivitu učenia a~zlepšuje konvergenciu. Napr.\ \emph{AdaGrad}, \emph{RMSProp}, \emph{Adam} (v súčasnosti najpoužívanejší). Ich výhodou je dobrá stabilita a~rýchle učenie aj v~ťažko optimalizovateľných sieťach.
          \item \emph{Metóda konjugovaných gradientov}~--~je vhodná pre nelineárne a~veľké optimalizačné úlohy. Namiesto toho, aby sa váhy aktualizovali iba v~smere gradientu, táto metóda kombinuje predchádzajúce smery gradientov, aby sa vytvoril nový, efektívnejší smer. Výhodou je, že môže výrazne zrýchliť konvergenciu a~vyhnúť sa osciláciám. Na druhej strane, pre výpočtovú náročnosť je vhodnejšia skôr pre menšie siete alebo ako súčasť učenia po dávkach.
          \item \emph{Normalizácia po dávkach} (angl.\ \emph{batch normalization})~--~normalizuje vstupy každej vrstvy v~rámci jednej dávky tak, aby mali nulový priemer a~jednotkovú varianciu. Tým sa potláča nežiaduce kolísanie rozdelenia vstupných hodnôt spôsobené priebežným menením váh vo vrstvách. Výsledkom je rýchlejšia a~stabilnejšia konvergencia počas učenia, ako aj zmiernenie problémov s~miznúcim gradientom. Normalizácia v~rámci dávky tiež často umožňuje použiť vyššiu rýchlosť učenia a~znižuje závislosť na počiatočnom nastavení váh. Je však menej účinná pri veľmi malých dávkach alebo v~rekurentných sieťach.
        \end{enumerate}

      \subsubsection{Ťažkosti s~konvergenciou} \

        Zvlášť v~prípade hlbokých neurónových sietí sa vyskytuje problém s~konvergenciou účelovej funkcie, keďže veľký počet skrytých vrstiev spôsobuje ťažší tok gradientov cez sieť. Čiastočne tento problém súvisí s~miznutím gradientov, ale má svoje osobitné špecifiká~\cite{aggarwalNeuralNetworksDeep2018}.

        Jedným so spôsobov riešenia sú tzv.\ \emph{bránové siete}. Využívajú špecifické neuróny (bunky), obsahujúce adaptívne riadiace mechanizmy (brány), ktoré selektívne kontrolujú tok informácie v~sieti. Ich cieľom je zabezpečiť, aby sa relevantné informácie udržali a~prenášali počas spracovania vstupu, zatiaľ čo menej dôležité sa potláčajú. Tento prístup je kľúčový najmä pri modelovaní sekvenčných dát (napr.\ spracovanie textu, videa), kde dlhodobé závislosti často bránia efektívnemu učeniu. Takéto siete sa radia medzi rekurentné neurónové siete a~patria tu napr.\ \acrshort{lstm} či \acrshort{gru} (bližšie o~nich v~časti~\ref{ssec:rnn}).
        
        Ďalšou možnosťou sú \emph{reziduálne siete}, ktoré zavádzajú reziduálne prepojenia, teda obchádzajúce vetvy, ktoré umožňujú prenášať informáciu aj gradient priamo naprieč viacerými vrstvami bez ich transformácie. Tým sa výrazne znižuje riziko miznutia gradientu a~zlepšuje sa stabilita učenia, čo umožňuje trénovať siete s~desiatkami až stovkami vrstiev. Tento prístup zároveň urýchľuje konvergenciu a~vedie k~lepšiemu využitiu kapacity modelu. Známy príklad takejto architektúry je ResNet~\cite{heDeepResidualLearning2016}. Princíp fungovania reziduálnych sietí spočíva v~tom, že namiesto učenia sa váh, teda priamej transformácie vstupu na výstup, sa učí len rozdiel medzi vstupom a~výstupom. Takéto prepojenie umožňuje gradientom efektívne pretekať aj do veľmi hlbokých vrstiev siete počas spätného šírenia chyby.

      \subsubsection{Lokálne a~falošné optimá}
        
      Optimalizačná funkcia neurónových sietí je silne nelineárna a~obsahuje veľké množstvo lokálnych extrémov. Preto je výber vhodnej inicializácie parametrov kľúčový. Nesprávne inicializačné body môžu viesť k~nevhodným (lokálnym alebo falošným) minimám. Jednou z~metód, ako sa tomu vyhnúť, je tzv.\ \emph{predtrénovanie} (angl.\ \emph{pretraining}), ktoré spočíva v~postupnom trénovaní jednotlivých vrstiev siete, často pomocou učenia bez učiteľa. Tento prístup poskytuje rozumné počiatočné hodnoty váh, ktoré obchádzajú irelevantné časti parametrového priestoru. Predtrénovanie tiež pomáha znižovať riziko pretrénovania, keďže sa vyhýba minimám, ktoré sú síce vhodné pre trénovaciu množinu, no zlyhávajú na testovacích dátach~--~tzv.\ falošným optimám~\cite{aggarwalNeuralNetworksDeep2018}.

      \subsubsection{Výpočtová náročnosť}

        \noindent Jednou z~hlavných výziev pri návrhu neurónových sietí je dlhá doba trénovania, ktorá môže v~oblastiach ako spracovanie textu či obrazu trvať aj celé týždne. Kľúčový pokrok v~tomto smere priniesol vývoj výkonnejšej hardvérovej infraštruktúry, najmä grafických procesorov (\acrshort{gpu}), ktoré sú schopné paralelne vykonávať výpočty typické pre neurónové siete. Platformy ako Torch alebo TensorFlow majú vstavanú podporu pre \acrshort{gpu}, čo umožňuje efektívnejšie učenie aj zložitých architektúr. Hoci k~pokroku v~hlbokom učení prispeli aj algoritmické vylepšenia, značná časť úspechu spočíva v~tom, že existujúce algoritmy dnes dokážeme lepšie využiť vďaka výkonnejšiemu hardvéru.Výhodou väčšiny neurónových sietí je, že náročné výpočty prebiehajú počas trénovania, zatiaľ čo predikčná fáza je už relatívne rýchla, čo je dôležité pri aplikáciách v~reálnom čase~\cite{aggarwalNeuralNetworksDeep2018}.
  
  
    \subsection{Súčasné prístupy k~trénovaniu}

      Snaha o~vyriešenie vyššie spomenutých problémov trénovania \acrshort{uns} podnietila vznik množstva variácií učiacich algoritmov, ktoré sú síce v~základoch postavené na metódach gradientného zostupu a~spätného šírenia chyby, ale obsahujú aj ďalšie modifikácie a~vylepšenia, týkajúce sa či už len samotného procesu trénovania, alebo aj architektúry celej siete, resp.\ jej buniek.

      Jedným z~používaných vylepšení učenia je tzv.\ \emph{prenos znalostí} (angl.\ \emph{transfer learning}). Spočíva v~tom, že sieť sa netrénuje od nuly, ale vychádza sa z~už predtrénovaného modelu, ktorý bol učený na rozsiahlej množine údajov. Tento model sa následne \uv{dolaďuje} (tzv.\ \emph{fine-tuning}) na konkrétnu úlohu pomocou menšieho množstva nových dát. Transferové učenie výrazne znižuje nároky na dáta aj výpočtový čas a~zároveň zvyšuje kvalitu výsledkov, najmä ak sa pracuje s~málo rozsiahlymi alebo špecializovanými údajmi pre konkrétnu oblasť. Táto metóda je dnes základom mnohých aplikácií~--~od spracovania prirodzeného jazyka (napr.\ BERT, GPT) až po analýzu obrazov (napr.\ ResNet, EfficientNet).

      Ďalším riešením pre zníženie výpočtovej náročnosti je \emph{trénovanie so zmiešanou číselnou presnosťou} (angl.\ \emph{mixed precission}). V~tomto prístupe prebiehajú niektoré výpočty počas trénovania s~nižšou číselnou presnosťou (napr.\ 16-bitové čísla namiesto štandardných 32-bitových). Výsledkom je nižšia pamäťová záťaž a~výrazne rýchlejšie výpočty, najmä na grafických procesoroch (GPU) a~špecializovaných akcelerátoroch (TPU), ktoré sú pre takéto výpočty optimalizované. Táto technika si zachováva presnosť trénovania pomocou špeciálnych mechanizmov (napr.\ škálovanie gradientov) a~dnes je často využívaná v~priemyselnej praxi.

      Trénovanie pomocou klasickej metódy gradientného zostupu býva pri zložitejších architektúrach neefektívne, pretože používa konštantnú rýchlosť učenia pre všetky parametre siete po celú dobu procesu trénovania. Veľká rýchlosť je totiž dobrá na rýchle priblíženie sa do blízkosti dobrého riešenia, avšak zväčša neumožňuje jemné doladenia sa ku optimu, teda sa môže stať, že algoritmus trénovania ostane oscilovať v~okolí optimálneho bodu alebo dokonca zdiverguje do nestabilnej oblasti. Na druhej strane, voľba malej rýchlosti učenia má za následok veľmi pomalú konvergenciu učenia. To sú dôvody pre vznik tzv.\ adaptívnych optimalizačných algoritmov, ktoré rýchlosť učenia dynamicky prispôsobujú pre každý parameter siete zvlášť počas celého procesu trénovania. 

      Jedným z~najrozšírenejších algoritmov pre trénovanie hlbokých neurónových sietí v~súčasnosti je Adam (Adaptive Moment Estimation)~\cite{kingmaAdamMethodStochastic2017}. Využíva adaptívne nastavovanie parametrov siete (váh a~biasov) na základe priebežných štatistík gradientu, ich prvého momentu (priemeru), ktorý informuje o~\uv{smerovaní} optimalizácie a~druhého momentu (rozptylu). Zároveň využíva tzv. L2 regularizačný člen, ktorý má za cieľ udržiavať hodnoty váh blízke nule (angl.\ \emph{weight decay}). Výsledný vzorec pre aktualizáciu $i$-teho parametru má tvar
      \begin{equation}
        w_i \:\leftarrow\: w_i - \frac{\alpha_t}{\sqrt{A_i}} F_i ;\ \forall i,
      \end{equation}
      kde agregovaný člen prvého momentu $F_i$, agregovaný člen druhého momentu $A_i$ a~rýchlosť učenia $\alpha_t$ sú priebežne aktualizované na základe nasledujúcich vzťahov~\cite{aggarwalNeuralNetworksDeep2018}:

      \begin{subequations}
        \begin{align}
          F_i &\:\leftarrow\: \rho_f F_i + (1 - \rho_f) \left( \frac{\partial\, \mathcal{L}}{\partial\, \mathcal{w_i}} \right);\ \forall i, \\
          A_i &\:\leftarrow\: \rho A_i + (1 - \rho) {\left( \frac{\partial\, \mathcal{L}}{\partial\, \mathcal{w_i}} \right)}^2;\ \forall i, \\
          \alpha_t &= \alpha \left( \frac{\sqrt{1 - \rho^t}}{1 - \rho_f^t} \right),
        \end{align}
      \end{subequations}
      pričom $\rho, \rho_f \in (0,1)$ sú voliteľné parametre. Novšia verzia algoritmu, AdamW~\cite{loshchilovDecoupledWeightDecay2019} vylepšuje pôvodný Adam v~tom, že regularizačný člen nemieša s~výpočtom gradientného momentu, ale počíta ho osobitne, čo vedie k~stabilnejšiemu trénovaniu a~lepšiemu generalizačnému výkonu siete.
  
\section{Hlboké učenie}

  \emph{Hlboké učenie} (angl.\ \emph{deep learning}) predstavuje jednu z najdôležitejších súčasných oblastí vývoja \acrshort{ui}. Prekonáva ťažkosti klasických perceptrónových sietí v riešení komplexnejších úloh využitím viacvrstvových neurónových architektúr, kde každá vrstva extrahuje čoraz abstraktnejšie znaky z~výstupov predchádzajúcich vrstiev. Vďaka tejto hierarchickej reprezentácii je možné modelovať zložité funkcie priamo z~dát bez nutnosti manuálne definovaných príznakov~\cite{lecunDeepLearning2015}. Hlboké siete tak výrazne prevyšujú tradičné \acrshort{mlp} v~schopnosti učiť sa reprezentácie priamo zo surových dát, čím umožňujú riešiť zložité úlohy ako rozpoznávanie objektov, preklad jazykov alebo predikciu dynamiky systémov.

  Na rozdiel od plytkých sietí vyžaduje trénovanie hlbokých modelov pokročilejšie optimalizačné techniky. Kým \acrshort{mlp} siete často využívali obyčajný gradientný zostup s~fixnou rýchlosťou učenia, hlboké siete zvyknú používať algoritmy ako Adam, RMSProp alebo momentové metódy, ktoré zohľadňujú historické hodnoty gradientov a~adaptívne upravujú parametre učenia~\cite{kingmaAdamMethodStochastic2017}. Za účelom predchádzania vyššie spomenutým problémom, ako je miznúci a~vybuchujúci gradient, sa zároveň používajú metódy ako regularizácia (napr. L2 penalizácia alebo \uv{dropout}), váhová inicializácia a~plánovanie rýchlosti učenia~\cite{goodfellowDeepLearning2016}.

  % V~tejto časti sa zameriame predovšetkým na dve významné architektúry:
  % \begin{itemize}
  %   \item \emph{Konvolučné neurónové siete (CNN)}, ktoré sú optimalizované na spracovanie štruktúrovaných vstupov (napr.\ obrazových dát, výstupov z~lidarov, topografických máp),
  %   \item \emph{Rekurentné neurónové siete (RNN)}, ktoré sú vhodné na modelovanie sekvenčných údajov a~nachádzajú využitie pri spracovaní senzorických tokov, predikcii pohybu či fúzii dát z~viacerých senzorov s~časovou závislosťou.
  % \end{itemize}

  \subsection{Konvolučné neurónové siete}\label{ssec:cnn}

    Konvolučné neurónové siete (\acrshort{cnn}) sú špeciálne navrhnuté na efektívne spracovanie dát so štruktúrou v~priestore. Hoci boli vynájdené ešte koncom 20.\ storočia, do širšieho používania sa dostali až po práci kolektívu A. Krizhevsky~\cite{krizhevskyImageNetClassificationDeep2012} a~dodnes sú najúspešnejším nástrojom na rozpoznávanie a~klasifikáciu obrazu z~kamery. Oproti klasickým plne prepojeným sieťam majú pri rovnakom počte neurónov menej prepojení, vďaka čomu sú jednoduchšie na natrénovanie a~odolnejšie voči pretrénovaniu. Princíp ich fungovania je inšpirovaný fungovaním mozgu cicavcov pri spracovaní zrakových vnemov~\cite{mehligMachineLearningNeural2021}.

    Vstupom do \acrshort{cnn} je obvykle obraz vo formáte dvojrozmernej bitmapy. Pri rozpoznávaní obrazu sa v~niekoľkých iteráciách opakuje postup, kedy sa na vstupnú vrstvu neurónov aplikujú konvolučné filtre, tzv.\ \emph{jadrá} (angl.\ \emph{kernels}), pričom každé z~nich obsahuje počet neurónov rádovo nižší než je rozmer vstupnej vrstvy (napr.\ $3 \times 3$, $5 \times 5$, $8 \times 8$, \dots). Jadro postupne aplikované na celú vstupnú vrstvu, pričom sa vykonáva operácia konvolúcie. Výstup jedného vstupného neurónu tak možno vyjadriť ako
    \begin{equation}
      V(i,j) = f\left( \sum_{p=1}^{P} \sum_{q=1}^{Q} x\, (i+p-1,j+q-1)\cdot w\, (p,q) - \theta \right),
    \end{equation}
    kde $i,j$ sú súradnice vstupného neurónu, $w(p,q)$ je hodnota jedného neurónu jadra s~rozmermi $P \times Q$ a~$x(i+p,j+q)$ je vstupná hodnota v~okolí daného vstupného neurónu a~$f(\cdot)$ predstavuje nelineárnu aktivačnú funkciu (obvykle \acrshort{relu})~\cite{mehligMachineLearningNeural2021}.
    
    V~procese učenia sú váhy jadier nastavené tak, aby rozpoznávali určité príznaky vo vstupných dátach (napr.\ hrany, textúry, objekty), čím vznikajú tzv.\ \emph{mapy príznakov} (angl.\ \emph{feature maps}). Vlastnosťou tejto techniky je invariantnosť voči posunu, zmene mierky alebo iným formám skreslenia, vďaka čomu je použitím jedného malého jadra možné detegovať ten istý príznak vo viacerých častiach vstupu bez zmeny jeho hodnôt. Významne sa tak redukuje počet parametrov potrebných na natrénovanie takejto siete oproti plnému prepojeniu. Tento proces sa opakuje vo viacerých po sebe nasledujúcich \emph{konvolučných vrstvách}, pričom platí, že čím viac vrstiev sa použije, tým komplexnejšie príznaky je možné rozpoznávať. Na učenie konvolučných vrstiev sa používa algoritmus spätného šírenia chyby~\cite{mehligMachineLearningNeural2021}.

    Medzi jednotlivé konvolučné vrstvy sa umiestňujú tzv.\ \emph{zhlukovacie vrstvy} (angl.\ \emph{pooling layers}). Ich vstupom sú výstupy z~viacerých susedných máp príznakov a~redukuje ich na jediné číslo. Tieto vrstvy neobsahuje žiadne parametre, ktoré by boli predmetom trénovania. Úlohou je podvzorkovanie mapy príznakov, na čo sa obvykle využíva funkcia maxima alebo priemeru. Výstup zo zhlukovacích vrstiev slúži ako vstup pre nasledujúce konvolučné vrstvy. Napokon, výsledná klasifikácia \acrshort{cnn} môže byť vykonaná pomocou \emph{plne prepojených vrstiev}.

    \begin{figure}[t!]
      \centering
      \includegraphics[width=0.8\textwidth]{img/cnn.png}
      \caption{Schéma architektúry konvolučnej neurónovej siete.~\cite{mehligMachineLearningNeural2021}}
    \end{figure}

    Medzi najvyužívanejšie implementácie patria TensorFlow a~PyTorch, ktoré ponúkajú predpripravené moduly na tvorbu a~trénovanie \acrshort{cnn}, ako aj množstvo predtrénovaných modelov (napr.\ VGG, ResNet, MobileNet či EfficientNet) vhodných na prenos znalostí. Tieto knižnice podporujú aj hardvérovú akceleráciu (napr.\ pomocou \acrshort{gpu}), čo umožňuje efektívne trénovanie rozsiahlych sietí. V~praxi sa \acrshort{cnn} implementácie uplatňujú v~prostrediach ako OpenCV, ktoré ponúka jednoduché rozhranie na inferenciu \acrshort{cnn} modelov v~reálnom čase a~je často využívaný vo vnorených systémoch a~robotike. Vďaka týmto nástrojom je dnes práca s~\acrshort{cnn} prístupná aj bez potreby vlastnej implementácie architektúr od základu.

  \subsection{Rekurentné neurónové siete}\label{ssec:rnn}
  
    Rekurentné neurónové siete (\acrshort{rnn}) tvoria triedu architektúr umelých neurónových sietí navrhnutých špecificky na spracovanie sekvenčných dát. Na rozdiel od bežných dopredných sietí, ktoré spracúvajú vstupy ako nezávislé a~nemajú vnútornú pamäť, \acrshort{rnn} obsahujú mechanizmus \emph{rekurencie}, ktorý im umožňuje uchovávať informácie o~predchádzajúcich vstupoch v~tzv.\ \emph{skrytom stave}. Táto schopnosť ich predurčuje na úlohy, v~ktorých poradie alebo časový kontext zohráva rozhodujúcu úlohu~--~napríklad pri spracovaní textu, zvuku, signálov zo senzorov či trajektórií v~priestore.

    Tento typ neurónových sietí neobsahuje iba dopredné väzby, teda zľava doprava, ale obsahujú tiež spätné väzby a~pamäťové informácie o~stave buniek. Zásadným rozdielom \acrshort{rnn} oproti klasickým dopredným sieťam je tiež to, že vstupy nie sú pripojené priamo na prvú skrytú vrstvu, ale interagujú so skrytými stavmi, ktoré boli vypočítané na základe predošlých vstupov. Skrytý stav siete sa mení v~čase a~je reprezentovaný vektorom $h_t$, ktorého výpočet je funkciou aktuálneho vstupného vektora $X_t$ a~predošlého skrytého stavu $h_{t-1}$:
    \begin{equation}
      H_t = \sigma\, (W_{xh}\, X_t + W_{hh}\, H_{t-1} + b_h),
    \end{equation}
    kde $W_{xh}$ a~$W_{hh}$ sú váhové matice pre vstup a~skrytý stav, $b_h$ je bias a~$\sigma$ je aktivačná funkcia (najčastejšie hyperbolický tangens alebo \acrshort{relu}). Na výpočet výstupu siete sa potom väčšinu používa aktivačná funkcia softmax alebo sigmoid. Výstup siete môže byť generovaný za každou vstupnou vzorkou alebo až po ukončení jednej dávky. Aktivačné funkcie sú rovnaké pre každú vstupnú vzorku.

    \begin{figure}[b!]
      \centering
      \input{img/rnn.tex}
      \caption{Rekurentná neurónová sieť~--~znázornenie toku informácií v~čase.}
    \end{figure}

    Vďaka schopnosti modelovať dlhodobé závislosti v~čase sú \acrshort{rnn} vhodné na spracovanie senzorických dát, kde každé meranie tvorí súčasť väčšej sekvencie. Môžu slúžiť aj ako časový filter, nakoľko dokážu integrovať informácie z~predchádzajúcich meraní a~korigovať neistotu v~aktuálnych pozorovaniach. K~ich výhodám patrí aj prispôsobivosť a~univerzálnosť, keďže tá istá architektúra sa dá použiť na rôzne typy sekvencií. Sú však náročné na trénovanie a~citlivé na dĺžku a~kvalitu sekvencie. Medzi najčastejšie používané implementácie \acrshort{rnn} patria tieto architektúry:
      \begin{itemize}
        \item \emph{Dlhá krátkodobá pamäť (\acrshort{lstm})}~\cite{hochreiterLongShortTermMemory1997}~--~(obr.~\ref{fig:lstm}) zavádza vnútorný pamäťový stav bunky a~sústavu brán, ktoré regulujú tok informácií: vstupná brána rozhoduje, čo nové si zapamätať, výstupná brána rozhoduje, ktorú informáciu z~pamäte použiť ako výstup a~zabúdacia brána rozhoduje, čo z~pamäte vyhodiť. Prináša teda schopnosť udržať si informáciu z~minulosti bez toho, aby došlo k~miznutiu gradientu. 
        \item \emph{Rekurentná jednotka s~bránou (\acrshort{gru})}~\cite{chungEmpiricalEvaluationGated2014}~--~(obr.~\ref{fig:gru}) ide o~zjednodušenú verziu \acrshort{lstm}, ktorá obsahuje len dve brány: aktualizačná~--~riadi, koľko z~predchádzajúceho stavu sa prenesie ďalej~--~a resetovacia~--~rozhoduje, koľko informácie z~minulosti ignorovať. Je výpočtovo efektívnejšia než \acrshort{lstm}, no výkonovo porovnateľná.
      \end{itemize}

    \begin{figure}[bh!]
      \centering
      \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{img/lstm.png}
        \caption{LSTM sieť~--~zabúdacia, vstupná a~výstupná brána}\label{fig:lstm}
      \end{subfigure}
      \hfill
      \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{img/gru.png}
        \caption{GRU sieť~--~aktualizačná a~resetovacia brána}\label{fig:gru}
      \end{subfigure}
      \caption{Porovnanie štruktúr LSTM a~GRU rekurentných neurónových sietí.~\cite{jiangMixedDeepRecurrent2019}}
    \end{figure}

% \section{Strojové učenie}

%   Strojové učenie sa využíva na aplikácie, ktoré kvôli svojej komplexite nie je možné naprogramovať tak, aby bol riadiaci program schopný optimálne reagovať na všetky možné stavy, ktoré môžu nastať. Týmto spôsobom prebieha postupná adaptácia na reálne podmienky na základe spätnej väzby zo systému. Najzákladnejšie typy strojového učenia sú:

%   \subsection{Učenie s~posilňovaním}

%   \subsection{Hlboké učenie}

% \section{Fuzzy logika}

\section{Fúzia lokalizácie pomocou UNS}
  Presná znalosť polohy v~prostredí~--~lokalizácia~--~predstavuje jeden zo základných predpokladov pre spoľahlivú prevádzku a~efektívne plnenie úloh mobilného robota. Na tento účel sa bežne využíva viacero typov senzorov, ako napríklad \acrshort{gnss}, \acrshort{ins}, vizuálne systémy alebo laserové snímače. Každý z~týchto senzorov však podlieha určitým obmedzeniam a~v~praxi neposkytuje dokonale presné ani spoľahlivé údaje. Na zníženie neistoty a~zvýšenie robustnosti odhadu aktuálnej polohy sa preto využíva fúzia údajov, t.\ j.\ inteligentná integrácia viacerých dátových tokov do jedného spoločného odhadu, ktorý je spravidla presnejší a~konzistentnejší než jednotlivé zdroje samostatne.

  Tradičné prístupy k~fúzii údajov sú založené prevažne na pravdepodobnostných modeloch (napr. Bayesovská filtrácia, Kalmanove filtre, časticové filtre) alebo metódach intervalového odhadu. Najväčšou výzvou pri použití týchto metód je však získanie presného modelu systému. V~prípade komplexných a~vysoko dynamických systémov môže byť odvodenie presného modelu zvlášť náročné. Navyše, senzorické dáta bývajú sprevádzané vlastnou nepresnosťou a~neistotou, ktorých zapracovanie do modelu je problematické. Vo výsledku tak nepresný model systému môže vážne obmedziť úspešnosť analytických metód fúzie~\cite{fayyadDeepLearningSensor2020}. 
  
  S~nástupom moderných techník umelej inteligencie~--~predovšetkým umelých neurónových sietí a~hlbokého učenia~--~sa postupne presadzuje aj ich uplatnenie v~tejto oblasti. Tieto modely sú schopné spracovávať veľké objemy vysoko dimenzionálnych dát, učiť sa zložité nelineárne vzťahy medzi jednotlivými vstupmi a~zohľadňovať aj ich časové závislosti bez hlbokej analytickej znalosti modelu, čo z~nich robí atraktívny nástroj pre riešenie úloh lokalizácie prostredníctvom senzorickej fúzie.

  Príkladom využitia štandardnej, plne prepojenej \acrshort{dffn} siete na fúziu lokalizácie môže byť práca Barreto-Cuberto a~kol~\cite{barreto-cuberoSensorDataFusion2022}. Využíva sa tu kombinácia troch typov senzorov: 360-stupňový 2D lidar, stereokamera a~ultrazvukové senzory. Úlohou neurónovej siete bolo zlepšiť schopnosť mobilného robota detegovať rôzne druhy prekážok vrátane sklenených povrchov, ktoré sú pre lidar často neviditeľné. Každý z~týchto senzorov má iné výhody: ultrazvuk vie spoľahlivo detegovať sklo, stereo kamera zachytáva trojrozmernú hĺbkovú štruktúru a~lidar poskytuje vysoké rozlíšenie v~2D rovine. Pred fúziou sa surové senzorické dáta upravujú~--~aplikujú sa filtre (napr. Kalmanov filter na ultrazvuk), transformácie do spoločného súradnicového systému a~projekcia mračien bodov na 2D rovinu. Neurónová sieť bola trénovaná na predikciu spoľahlivých vzdialeností k~prekážkam spojením vstupov zo všetkých troch senzorov. Používa Adam optimalizátor, aktivačnú funkciu \acrshort{relu} a~ďalšie osvedčené techniky pre nelineárne dáta. Výsledkom fúzie je jednotná metrika vzdialeností, ktorú systém využíva na tvorbu 2D okupačnej mapy (angl.\ Occupancy Grid Map). Experimentálne porovnanie viacerých stratégií (lidar-sólo, lidar+sonar, lidar+kamera+sonar) ukázalo, že fúzia pomocou \acrshort{uns} výrazne znižuje strednú kvadratickú chybu (\acrshort{rmse}) (až pod 3~cm), pričom samotný lidar mal chybu cez 2 metre pri prítomnosti skla. Navyše, kombinácia kamery a~sonaru umožňuje rozpoznať aj objekty pod úrovňou skenovania lidaru, čím sa zvyšuje bezpečnosť a~spoľahlivosť navigácie.

  \begin{figure}[t!]
    \centering
    \includegraphics[width=0.6\textwidth]{img/mlp_fusion.png}
    \caption{Fúzia senzorov pomocou \acrshort{dffn}~\cite{barreto-cuberoSensorDataFusion2022}}
  \end{figure}

  \subsection{CNN fúzia} \

    Nakoľko je architektúra \acrshort{cnn} sietí prispôsobená na spracovanie dvojrozmerných dát, v~súvislosti s~fúziou sa ich využitie objavuje najčastejšie pri spracovaní kamerového obrazu, príp.\ máp.
    
    Jednou z~prvých implementácií metód hlbokého učenia na účely fúzie odometrie vôbec je práca autorov Valente a~kol.\ z~roku 2019~\cite{valenteDeepSensorFusion2019}, ktorí sa zaoberali fúziou monokamery a~2D laserového skenera. Cieľom bolo eliminovať závislosť od presnej kalibrácie medzi senzormi a~zložitosť návrhu ručne definovaných fúznych pravidiel. Navrhnutý prístup transformuje klasický regresný problém odometrie (odhadu vzdialenosti a~natočenia medzi snímkami) na problém ordinálnej klasifikácie, čím zjednodušuje učenie siete. Vstupom do siete sú vždy dvojice po sebe idúcich 360° laserových skenov a~obrázkov z~kamery, pričom sieť predikuje transformáciu ${(\Delta d, \Delta \theta)}^{\top}$~--~teda prejdenú vzdialenosť a~rotačný uhol medzi dvoma časovými krokmi. Akumuláciou týchto lokálnych odhadov je možné rekurzívne rekonštruovať globálnu trajektóriu robota. Architektúra pozostáva z~dvoch vetiev (\acrshort{cnn} pre laser, \acrshort{cnn} pre kameru), ktorých výstupy sú zlúčené a~prevedené na odhad translácie a~rotácie. Výsledky na reálnych dátach z~KITTI datasetu ukazujú, že konvolučné siete dokážu efektívne fúzovať heterogénne senzorické údaje pre účely lokalizácie v~reálnom čase. Tento prístup je preto vhodný najmä v~situáciách, kde nie sú dostupné kinematické dáta (napr.\ z~enkodérov) alebo \acrshort{gnss}\@.

    Efektívne využitie \acrshort{cnn} technológie je predstavené v~článku~\cite{xiaoDynamicSLAMSemanticMonocular2019}, kde autori predstavujú systém Dynamic-SLAM, ktorý rozširuje klasické vizuálne \acrshort{slam} riešenia tak, aby dokázali spoľahlivo pracovať v~dynamickom prostredí. Kým tradičné \acrshort{slam} metódy predpokladajú, že všetky pozorované prvky sú statické, tento prístup využíva sémantickú segmentáciu pomocou \acrshort{ssd} konvolučnej neurónovej siete na rozpoznávanie dynamických objektov v~obraze. Tieto objekty sa detegujú v~samostatnom vlákne a~na základe toho sa ich zodpovedajúce obrazové prvky (napr.\ rohové body) vylúčia z~odhadu pohybu robota, čím sa výrazne znižuje chyba lokalizácie. Keďže \acrshort{ssd} môže niektoré objekty vynechať, autori navrhujú kompenzačný algoritmus, ktorý využíva fakt, že pohyb objektov medzi po sebe idúcimi snímkami býva plynulý~--~tzv.\ rýchlostnú invariantnosť~--~a na základe toho spätne identifikuje nedetekované objekty. Následne sa v~sledovacom vlákne aplikuje selektívne sledovanie, ktoré spracúva len spoľahlivé statické prvky. Výsledný systém je robustnejší voči narušeniu dynamikou prostredia, funguje v~reálnom čase, a~experimentálne preukazuje vyššiu presnosť a~úspešnosť lokalizácie než existujúce riešenia ako ORB-SLAM2 či DynaSLAM, a~to nielen v~simulovaných testoch, ale aj na mobilnej robotickej platforme v~reálnom svete.

    % - [\cite{matsuguSubjectIndependentFacial2003},\cite{gaoObjectClassificationUsing2018}]: CNN na odhad hĺbky z~monokulárnej kamery

    DeepFusion je riešenie predstavené v~práci~\cite{laidlowDeepFusionRealTimeDense2022}, ktoré systém pre hustú 3D rekonštrukciu z~monokulárneho videa. Kombinuje výhody geometrických metód a~hlbokého učenia. Motiváciou je prekonanie obmedzení existujúcich metód, ako je obmedzený dosah hĺbkových kamier a~nejednoznačnosť mierky pri rekonštrukcii z~fotometrických chýb, čo je kľúčové pre robotické úlohy. Na rozdiel od klasických \acrshort{slam} systémov, ktoré vytvárajú len riedke mapy na základe kľúčových bodov, DeepFusion generuje husté hĺbkové mapy v~mierke pomocou \acrshort{cnn}, ktorá predikuje nielen hĺbku z~jedného obrazu, ale aj jej gradienty a~neistoty. Tieto výstupy sa potom pravdepodobnostne spájajú so čiastočne hustou rekonštrukciou z~viacerých pohľadov, čím sa zachováva globálna konzistencia aj absolútna mierka. Optimalizácia prebieha priebežne počas prechodu novými snímkami, ale sieť sa spúšťa len raz pre každý kľúčový snímok, čím sa zachováva reálny čas spracovania na \acrshort{gpu}. Tento hybridný prístup využíva prednosti učenia (presnosť vo vnútri objektov) aj geometrie (ostré hrany) a~podľa experimentov dosahuje porovnateľné alebo lepšie výsledky ako iné existujúce systémy.
  
  \subsection{RNN fúzia} \

    Ako už bolo spomenuté v~časti~\ref{ssec:rnn}, silnou stránkou \acrshort{rnn} sietí je schopnosť spracúvať časovo-závislé dáta a~pamätať si z~nich vzniknuté závislosti. Vďaka tomu je táto architektúra vhodná na spracovanie meraní z~\acrshort{gnss} a~\acrshort{imu}. 

    Autori článku~\cite{daiINSGNSSIntegrated2020} reagujú na obmedzenia tradičných metód integrovanej navigácie \acrshort{ins}/\acrshort{gnss}, najmä rozšíreného Kalmanovho filtra (\acrshort{ekf}), ktorý si vyžaduje presný model chýb senzorov a~trpí zníženou presnosťou pri dlhodobom výpadku \acrshort{gnss} signálu v~silne nelineárnych alebo rušených prostrediach. Na rozdiel od statických neurónových sietí, ktoré nedokážu zachytiť časové závislosti chýb \acrshort{ins}, navrhujú autori využitie \acrshort{rnn} schopnej modelovať dynamické väzby medzi aktuálnymi a~minulými chybami \acrshort{ins}\@. \acrshort{rnn} sa trénuje na dátach z~obdobia, keď je \acrshort{gnss} dostupné, a~následne dokáže počas výpadku \acrshort{gnss} predikovať chyby polohy a~rýchlosti \acrshort{ins} na základe vlastnej \uv{pamäte} a~údajov o~dynamike vozidla (napr.\ zmeny rýchlosti a~orientácie). Tento prístup umožňuje kontinuálne a~presné určovanie polohy bez potreby zložitého modelovania alebo ladenia parametrov ako v~prípade \acrshort{ekf}. Výsledky zo simulácií aj reálnych experimentov (napr.\ počas 10-hodinovej plavby lode) ukázali, že navrhovaná metóda dosahuje výrazné zníženie chýb v~porovnaní s~\acrshort{ekf} (až o~61~\% v~polohe a~40~\% v~rýchlosti) aj s~\acrshort{elm}, čo potvrdzuje jej potenciál pre navigáciu v~prostrediach s~nedostupným alebo rušeným \acrshort{gnss} signálom.

    Podobný problém súvisiaci s~potenciálnym obmedzením dostupnosti a~presnosti \acrshort{gnss} signálu v~zastavaných mestských oblastiach či tuneloch sa rozhodli riešiť aj autori práce~\cite{kimDeepLearningBasedGNSS2019}. Na preklenutie výpadkov \acrshort{gnss} meraní sa rozhodli zapojiť do kombinácie aj údaje z~\acrshort{imu} a~kolesovej odometrie, no v~snahe vyhnúť sa potrebe komplexného odhadu modelu jednotlivých senzorov využili \acrshort{lstm} sieť. Model predikuje aktuálnu polohu na základe predchádzajúcich meraní zo všetkých spomenutých senzorov. Autori uvádzajú 40\%-tné zmenšenie chyby odhadu v~porovnaní so samostatnou \acrshort{gnss} navigáciou v~zjednodušených testovacích podmienkach. Napriek ukázanému potenciálu metód hlbokého učenia v~podobe obídenia procesu kalibrácie a~modelovania, riešenie by ešte potrebovalo validáciu v~náročnejších reálnych podmienkach.

    % Práca autorov Jiang a~kol.~\cite{jiangMixedDeepRecurrent2019} využíva kombinovanú \acrshort{lstm}+\acrshort{gru} sieť priamo na modelovanie šumu meraní \acrshort{ins}\@. Testované boli dve varianty architektúry: LSTM-GRU a~GRU-LSTM\@. V~oboch prípadoch sa vstupné dáta pre každú časovú vzorku najprv spracujú jednou rekurentnou jednotkou (\acrshort{lstm} alebo \acrshort{gru}), a~jej výstup sa potom okamžite odovzdá druhej jednotke. Zároveň každá z~jednotiek propaguje svoj vlastný stav naprieč časom (\acrshort{lstm} aj cez stav bunky, \acrshort{gru} len cez skrytý stav). Tento sériový spôsob spracovania umožňuje využiť výhody oboch mechanizmov~--~\acrshort{lstm} je vhodná pre zachovanie dlhodobých závislostí, zatiaľ čo \acrshort{gru} dosahuje dobrý výkon aj pri kratších sekvenciách a~s~menším počtom parametrov. Experimenty prebiehali v~statických podmienkach a~preukázali zníženie chyby odhadu orientácie vo všetkých troch osiach výraznejšie než pri použití samostatných \acrshort{lstm} alebo \acrshort{gru}. Architektúru by bolo ďalej žiadúce testovať v~dynamických podmienkach.

    Rekurzívne neurónové siete je možné využiť nie len na nahradenie analytických prístupov fúzie, ale aj na ich doplnenie, resp.\ riešenie problémov, na ktoré štandardné metódy nie sú stavané. Príkladom je nedávno publikovaná práca~\cite{huangApplicationMultisensorFusion2025a}, v~ktorej je fúzia pomocou \acrshort{ekf} doplnená o~\acrshort{rnn} sieť za účelom riešenia asynchrónnych frekvencií jednotlivých senzorov, akumulácie driftu a~šumu merania. Algoritmus prebieha v~troch krokoch. V~prvom je fúzia údajov z~\acrshort{imu} a~kolesovej odometrie zabezpečená pomocou \acrshort{ekf}, následne je použitá \acrshort{rnn} sieť na modelovanie časových závislostí, čo umožňuje vyhladzovanie dát a~redukciu driftu a~šumu, ktoré \acrshort{ekf} sám o~sebe nedokáže dobre potlačiť, najmä pri nelineárnych dynamikách alebo asynchrónnom snímaní\@. \acrshort{rnn} tak vystupuje ako korekčný mechanizmus, ktorý spracúva už \acrshort{ekf}-fúzované dáta a~zohľadňuje dlhodobejšie závislosti v~signáli. Následne sa výsledok koriguje pomocou komplementárnej fúzie s~lidarom pre zvýšenie robustnosti a~spoľahlivosti. Vďaka tejto architektúre algoritmus dosahuje lokalizačnú chybu do 8 cm a~prekračuje výkon metód ako časticový filter alebo \acrshort{slam} založený na grafovej optimalizácii polôh (tzv.\ Graph \acrshort{slam}) pri zachovaní výpočtovej efektivity, čím je vhodný aj pre reálne aplikácie v~autonómnej navigácii.